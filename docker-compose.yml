services:
  app:
    build: .
    container_name: ds881-curriculo-GRR20242220

    ports:
      - "8080:8080"

    volumes:
      - .:/app
      - /app/node_modules

    stdin_open: true
    tty: true